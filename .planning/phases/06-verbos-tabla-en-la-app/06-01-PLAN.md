---
phase: 06-verbos-tabla-en-la-app
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/components/ConjugationTable.tsx
  - app/src/components/ConjugationTable.test.tsx
  - app/src/views/Glosario.tsx
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "Abrir el detalle de un verbo muestra su tabla ます: 5 formas (dic + 4) con kana y romaji, etiquetadas en español"
    - "El grupo del verbo (ichidan / godan / irregular) es visible en la tabla"
    - "Los verbos irregulares muestran una marca visual distinta de los regulares"
    - "Abrir el detalle de un no-verbo (o un verbo sin datos de conjugación) no muestra tabla y no rompe la tarjeta"
  artifacts:
    - path: "app/src/components/ConjugationTable.tsx"
      provides: "Componente que renderiza un objeto Conjugation como tabla legible (5 formas + grupo + marca de irregular)"
      exports: ["default"]
      min_lines: 40
    - path: "app/src/components/ConjugationTable.test.tsx"
      provides: "Test jsdom del componente: etiquetas ES, kana+romaji por forma, marca de irregular, forma sin kanji"
    - path: "app/src/views/Glosario.tsx"
      provides: "VocabDetail renderiza ConjugationTable tras el guard item.conjugation; VocabRow marca los verbos"
      contains: "ConjugationTable"
  key_links:
    - from: "app/src/views/Glosario.tsx"
      to: "app/src/components/ConjugationTable.tsx"
      via: "render guardado en VocabDetail"
      pattern: "item\\.conjugation && <ConjugationTable"
    - from: "app/src/components/ConjugationTable.tsx"
      to: "app/src/components/SpeakerButton.tsx"
      via: "pronunciación por forma (fallback Web Speech, sin audioKey)"
      pattern: "SpeakerButton"
---

<objective>
Renderizar en el detalle de vocab (`VocabDetail` en `app/src/views/Glosario.tsx`) la
tabla de conjugación ます de los verbos, consumiendo el campo aditivo `conjugation`
que emite la Phase 5. SOLO frontend/render: NO se toca esquema, validadores ni
contenido.

Purpose: cerrar el bucle de la feature de verbos — el dato ya existe (Phase 5) pero
el usuario aún no lo ve; esta fase lo hace visible como tabla, con los irregulares
distinguidos, degradando con elegancia para no-verbos.
Output: un componente `ConjugationTable` reutilizable + su test, cableado en
`VocabDetail` bajo el guard `item.conjugation`, y una marca de "verbo" en `VocabRow`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-verbos-conjugaci-n-contenido-y-schema/05-02-SUMMARY.md

<interfaces>
<!-- Contratos que el executor necesita. Extraídos del codebase — NO explorar de nuevo. -->

De app/src/content/content.ts (tipos generados desde el schema — DO NOT edit):
```typescript
export interface Vocab {
  id: string
  type: "vocab"
  kanji: string
  kana: string
  romaji: string
  es: string
  pos?: string            // "verbo" para verbos
  tokens: [Token, ...Token[]]
  example?: Sentence
  conjugation?: Conjugation   // OPCIONAL — sólo presente en verbos (Phase 5)
  tags: [string, ...string[]]
}
export interface Conjugation {
  group: "ichidan" | "godan" | "irregular"
  irregular?: boolean
  dictionary: ConjForm
  masu: ConjForm
  masen: ConjForm
  mashita: ConjForm
  masendeshita: ConjForm
}
export interface ConjForm {
  kanji?: string   // frecuentemente ausente en ichidan (たべる, etc.)
  kana: string     // p.ej. "たべます"
  romaji: string   // p.ej. "tabemasu"
}
```

De app/src/components/SpeakerButton.tsx:
```typescript
// text: lo que se pronuncia; audioKey?: clave del manifiesto (OMITIR → sólo Web Speech);
// label?: variante barra vs icono 44px. Se OCULTA (return null) si no hay ni audio ni voz.
export default function SpeakerButton(props: { text: string; audioKey?: string; label?: string; className?: string }): JSX.Element | null
```

De app/src/tts/TtsContext.tsx:
```typescript
// useTts() tiene un valor DEFAULT no-op seguro (hasVoice:false, hasAudio:()=>false).
// => Un componente que use SpeakerButton renderiza SIN provider en tests: el botón
//    simplemente se oculta. No hace falta envolver el test en TtsProvider.
export function useTts(): { hasVoice: boolean; audioReady: boolean; hasAudio: (k: string) => boolean; speak: (t: string, k?: string) => void }
```

Datos reales de referencia (たべる, `2026-07-15:vocab:taberu`, group ichidan, sin kanji):
dictionary {kana:"たべる", romaji:"taberu"}, masu {"たべます","tabemasu"},
masen {"たべません","tabemasen"}, mashita {"たべました","tabemashita"},
masendeshita {"たべませんでした","tabemasen deshita"}.
べんきょうする es `irregular:true` + group "irregular".
</interfaces>

<style_notes>
<!-- Coherencia con el overlay existente (VocabDetail) y la tarjeta example. -->
- Tarjeta contenedora: `rounded-2xl bg-lavender/40 p-4` (idéntica a la de `item.example`).
- Kana japonés: usar `lang="ja"` + clase `jp-text` (las conjForm NO llevan tokens, así que
  kana plano + romaji basta — NO usar JapaneseText, que requiere tokens/modo).
- Encabezados de sección: `text-[14px] font-bold text-muted` (como "Kanji de esta palabra").
- Marca de irregular: pill distinta, p.ej. `bg-coral/20 text-coral` con `font-bold`.
- Etiqueta de grupo regular: pill neutra `bg-bg text-muted`.
- Mobile-first: tabla de una columna o filas apiladas (label a la izquierda, JA+romaji a la
  derecha); NO usar una `<table>` ancha que desborde en móvil — filas flex funcionan mejor.
</style_notes>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Componente ConjugationTable + test</name>
  <read_first>
    - app/src/content/content.ts (interfaces Conjugation, ConjForm, Vocab)
    - app/src/components/SpeakerButton.tsx (props; se oculta si no hay audio ni voz)
    - app/src/tts/TtsContext.tsx (useTts default no-op → testable sin provider)
    - app/src/views/Glosario.tsx líneas 106-119 (tarjeta example: rounded-2xl bg-lavender/40 p-4) y 121-123 (encabezado "Kanji de esta palabra")
    - app/src/display/ruby.test.tsx (patrón de test jsdom: `// @vitest-environment jsdom`, render de @testing-library/react)
  </read_first>
  <files>app/src/components/ConjugationTable.tsx, app/src/components/ConjugationTable.test.tsx</files>
  <behavior>
    - Renderiza las 5 formas EN ORDEN con estas etiquetas ES EXACTAS:
      dictionary → "Diccionario"; masu → "Presente (ます)"; masen → "Presente negativo (ません)";
      mashita → "Pasado (ました)"; masendeshita → "Pasado negativo (ませんでした)".
    - Cada fila muestra el kana (`lang="ja"`, clase `jp-text`) y el romaji de esa forma.
    - Muestra el grupo: ichidan → "Ichidan (grupo 2)"; godan → "Godan (grupo 1)"; irregular → "Irregular".
    - Marca de irregular VISIBLE (pill bg-coral/20) cuando `conjugation.irregular === true` O `conjugation.group === 'irregular'`; ausente para regulares.
    - Test 1: dado el objeto de たべる (ichidan), el contenedor contiene "Diccionario", "Presente (ます)", "たべます", "tabemasu", "Pasado negativo (ませんでした)", "たべませんでした" y NO contiene la pill de irregular.
    - Test 2: dado un objeto con group "irregular" (o irregular:true), aparece la marca de irregular (texto "Irregular").
    - Test 3: una forma sin `kanji` (todas las ichidan de ejemplo) renderiza su kana sin lanzar; el componente NO usa JapaneseText.
  </behavior>
  <action>
    Crear `app/src/components/ConjugationTable.tsx`:
    - `export default function ConjugationTable({ conjugation }: { conjugation: Conjugation })` (import type desde '../content/content').
    - Definir un array ordenado de `{ key, label }` con las 5 etiquetas ES exactas de arriba, mapeando cada `key` a `conjugation[key]` (ConjForm).
    - Contenedor: `<div className="mt-4 rounded-2xl bg-lavender/40 p-4">`.
    - Cabecera con título "Conjugación" (`text-[14px] font-bold text-muted`) + a su lado la pill de grupo y, si irregular, la pill "Irregular" (`bg-coral/20 text-coral`).
    - Cada forma como fila flex (`flex items-center justify-between gap-3`), con: a la izquierda el `label` ES (`text-[13px] text-muted`), a la derecha un bloque con el kana (`<span lang="ja" className="jp-text text-[17px] font-bold">{form.kana}</span>`) y debajo el `romaji` (`text-[13px] text-muted`).
    - Pronunciación por forma: `<SpeakerButton text={form.kana} />` (SIN audioKey → cae a Web Speech; se oculta solo si no hay voz ja-JP). Justificación: las formas de conjugación NO tienen audio pre-generado en v1 (Phase 5 notas), y SpeakerButton ya degrada con elegancia; añade recuerdo auditivo con riesgo nulo.
    - NO usar JapaneseText (las ConjForm no traen tokens ni modo de display).
    Crear `app/src/components/ConjugationTable.test.tsx` con `// @vitest-environment jsdom`, render de @testing-library/react, y los 3 tests de <behavior>. Renderizar SIN provider (useTts default no-op oculta SpeakerButton — verifica también la degradación).
  </action>
  <acceptance_criteria>
    - `cd app && npx tsc -b` sale con exit 0.
    - `cd app && npm test` verde (incluye los 3 nuevos tests de ConjugationTable).
    - `grep -c "Presente negativo (ません)" app/src/components/ConjugationTable.tsx` == 1 y las 5 etiquetas ES presentes.
    - `grep -c "JapaneseText" app/src/components/ConjugationTable.tsx` == 0 (no se usa).
    - `grep -c "SpeakerButton" app/src/components/ConjugationTable.tsx` >= 1.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc -b && npm test</automated>
  </verify>
  <done>El componente compila, los 3 tests pasan, las 5 etiquetas ES exactas están presentes, no usa JapaneseText y renderiza SpeakerButton por forma.</done>
</task>

<task type="auto">
  <name>Task 2: Cablear ConjugationTable en VocabDetail + marca de verbo en VocabRow</name>
  <read_first>
    - app/src/views/Glosario.tsx (VocabDetail ~79-158, guard de item.example ~106; VocabRow ~21-45)
    - app/src/components/ConjugationTable.tsx (creado en Task 1)
    - app/src/content/content.ts (Vocab.conjugation opcional)
  </read_first>
  <files>app/src/views/Glosario.tsx</files>
  <action>
    En `app/src/views/Glosario.tsx`:
    - Importar `ConjugationTable` desde '../components/ConjugationTable'.
    - En `VocabDetail`, tras el bloque `{item.example && (...)}` (línea ~119) y antes del bloque de kanji (`{chars.length > 0 && ...}`), insertar el render GUARDADO:
      `{item.conjugation && <ConjugationTable conjugation={item.conjugation} />}`.
      Esto garantiza la degradación elegante: un no-verbo o un verbo sin `conjugation` simplemente no muestra tabla.
    - En `VocabRow`, añadir una marca "verbo" trivial cuando `item.conjugation` exista: una pill pequeña junto al texto (p.ej. `<span className="ml-2 rounded-full bg-lavender/60 px-2 py-0.5 text-[11px] font-bold text-indigo">verbo</span>`) o un realce equivalente coherente con el estilo. Mantenerlo mínimo; si desborda el layout de la fila, colócalo bajo el `es`.
    - NO modificar VocabDetail más allá de la inserción guardada + import; NO tocar el flujo de kanji, example, ni el SpeakerButton de la palabra.
  </action>
  <acceptance_criteria>
    - `grep -c "item.conjugation && <ConjugationTable" app/src/views/Glosario.tsx` == 1.
    - `grep -c "ConjugationTable" app/src/views/Glosario.tsx` >= 2 (import + uso).
    - `cd app && npx tsc -b` exit 0; `cd app && npm test` verde (sin regresiones en la suite existente).
    - Degradación: el render de la tabla está tras `item.conjugation &&`, por lo que un vocab sin `conjugation` (no-verbo) nunca lo evalúa — razonamiento verificable por el grep del guard.
    - Verificación visual (ver <verify_visual>): el detalle de un verbo real muestra la tabla; un no-verbo no.
  </acceptance_criteria>
  <verify>
    <automated>cd app && npx tsc -b && npm test && grep -c "item.conjugation && <ConjugationTable" src/views/Glosario.tsx</automated>
  </verify>
  <verify_visual>
    Tras tsc+tests, correr `cd app && npm run dev` y comprobar en el navegador (viewport móvil):
    1. Glosario → clase 2026-07-15 → detalle de **たべる**: bajo el ejemplo aparece la tabla "Conjugación" con 5 filas —
       "Diccionario / たべる / taberu", "Presente (ます) / たべます / tabemasu",
       "Presente negativo (ません) / たべません / tabemasen", "Pasado (ました) / たべました / tabemashita",
       "Pasado negativo (ませんでした) / たべませんでした / tabemasen deshita"; grupo "Ichidan (grupo 2)", SIN pill de irregular.
    2. Detalle de **べんきょうする**: muestra la marca "Irregular".
    3. Detalle de un NO-verbo (sustantivo): NO aparece tabla y la tarjeta se ve normal.
    4. (Opcional) tocar la pronunciación de una forma: suena en japonés si hay voz ja-JP; si no, el botón no aparece (nunca voz equivocada).
  </verify_visual>
  <done>VocabDetail renderiza ConjugationTable bajo el guard item.conjugation; VocabRow marca los verbos; tsc y tests verdes; verificación visual del dev-server confirmada (たべる con tabla, べんきょうする irregular, no-verbo sin tabla).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content JSON → render | El contenido es dato local del repo, ya validado por la skill (ajv) antes de commit; entra al render de React. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering/Info | ConjugationTable render de kana/romaji | mitigate | Render sólo como texto React (auto-escapado); NO `dangerouslySetInnerHTML`; sin cómputo lingüístico en cliente. |
| T-06-02 | DoS | Objeto conjugation malformado | accept | El esquema+validate.mjs (Phase 5) garantizan las 5 formas presentes; el guard `item.conjugation` evita render sin dato. Dato local de confianza, sin superficie remota nueva. |
</threat_model>

<verification>
- `cd app && npx tsc -b` exit 0.
- `cd app && npm test` verde (suite completa, sin regresiones).
- `grep -c "item.conjugation && <ConjugationTable" app/src/views/Glosario.tsx` == 1 (guard de degradación).
- Checkpoint humano: たべる muestra tabla; べんきょうする marcado irregular; no-verbo sin tabla.
</verification>

<success_criteria>
1. El detalle de un verbo muestra su tabla ます (dic + 4 formas) con kana + romaji y etiquetas ES. (DISP-01)
2. Los verbos irregulares se distinguen visualmente de los regulares (pill "Irregular" + grupo).
3. Un no-verbo o un verbo sin `conjugation` no rompe: la tabla se omite por el guard `item.conjugation`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/06-verbos-tabla-en-la-app/06-01-SUMMARY.md`.
</output>
