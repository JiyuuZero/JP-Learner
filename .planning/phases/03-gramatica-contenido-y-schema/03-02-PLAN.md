---
phase: 03-gramatica-contenido-y-schema
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - content/classes/2026-07-03.json
  - content/classes/2026-07-06.json
  - content/classes/2026-07-08.json
  - content/classes/2026-07-10.json
  - content/classes/2026-07-13.json
  - content/index.json
autonomous: false
requirements: [CONT-01, CONT-02, SKILL-05]

must_haves:
  truths:
    - "Every grammar item in all 5 committed classes has, on each of its examples[], a word/bunsetsu-level tokens[] that satisfies the renderability invariants."
    - "Each grammar example marks exactly the taught particle/form token(s) with blank:true (the cloze answer)."
    - "Deterministic item IDs are UNCHANGED for every grammar item (SRS history preserved) — only tokens/blank are added; pattern/es/existing example text untouched except where a reading was wrong."
    - "Each class is re-committed via skill/commit-class.mjs after skill/validate.mjs passes and after MANDATORY human review of the tokenization/blank choices."
    - "generate-audio produces no NEW audio for grammar (grammar has no TTS today) — verify generated=0/unchanged per class; vocab audio untouched."
  artifacts:
    - path: "content/classes/2026-07-13.json"
      provides: "grammar examples carry tokens[]+blank (this class has 3 grammar items)"
      contains: "\"blank\""
    - path: "content/index.json"
      provides: "contentHash refreshed per re-committed class; single entry per classId"
  key_links:
    - from: "content/classes/*.json"
      to: "skill/validate.mjs"
      via: "fail-closed validation before every commit"
      pattern: "validate"
---

<objective>
Backfill the GRAMMAR of the 5 already-committed classes so their examples carry the
word-level tokens[] + blank marker Phase 4 needs — via the skill contract, one class at
a time, with mandatory human review. This is content work (human-in-the-loop), not an
autonomous edit: the tokenization and the choice of blank token are exactly what the
user must verify.
</objective>

<constraints>
- Touch ONLY grammar `examples[]` (add tokens[] + blank). Do NOT change pattern, es, or
  the example's kanji/kana/romaji/es text (unless a reading was actually wrong — flag it).
- Do NOT touch vocab/kanji/notes, and do NOT change any item `id` (SRS keys on id).
- Per class, run the same contract used for a new class: build candidate → node
  skill/validate.mjs → **show diff/candidate to user for review** → node
  skill/commit-class.mjs. Run generate-audio only to confirm it produces nothing new for
  grammar; re-commit only if audio actually changed.
</constraints>

<tasks>

### Task 1 — Inventory grammar per class
- List every grammar item + its examples across the 5 classes (e.g. 2026-07-13 has 3:
  jishokei-masukei, masu-negativo-pasado, no-encadenado; 2026-07-10 has 5; etc.).
- For each example, decide word-level chunking and which token is `blank` (the taught
  form/particle). Prepare a per-class candidate.

### Task 2 — Per class (×5): validate → HUMAN REVIEW → commit
For each of 2026-07-03, 2026-07-06, 2026-07-08, 2026-07-10, 2026-07-13:
1. Write the updated class JSON (grammar examples gain tokens[]+blank; everything else
   byte-identical).
2. `node skill/validate.mjs <class.json>` → must print `VALID <classId>`.
3. **MANDATORY human review**: present the grammar tokenization + blank choices; apply
   the user's corrections (authoritative). Do not commit without approval.
4. `node skill/commit-class.mjs <class.json>`.
5. `node skill/generate-audio.mjs <class.json>` → expect `generated=0` for grammar; if it
   changed anything, re-run commit-class. Confirm vocab audio unaffected.

### Task 3 — Final sanity
- Re-validate all 5 classes; confirm `content/index.json` has one entry per class with
  refreshed contentHash and no duplicate IDs.
- Confirm no vocab/kanji/note diffs and no id changes (git diff review).

</tasks>

<verification>
- All 5 classes validate; every grammar example has invariant-satisfying tokens[] and a
  blank marker on the taught token.
- git diff shows ONLY added tokens/blank on grammar examples (+ index.json hashes); no id
  changes, no vocab/kanji/note edits.
- App (Phase 4, later) can now build reorder + cloze from this data.
</verification>
