---
phase: 05-verbos-conjugaci-n-contenido-y-schema
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - content/schema/content.schema.json
  - skill/validate.mjs
  - app/src/content/validate.mjs
  - app/src/content/content.ts
  - skill/prompts/structure.md
autonomous: true
requirements: [DISP-01]

must_haves:
  truths:
    - "El schema define un campo OPCIONAL conjugation en vocab con group y las 5 formas (dictionary/masu/masen/mashita/masendeshita), cada forma con kana+romaji."
    - "El contenido existente (no-verbos y clases ya commiteadas) sigue validando contra el schema ajv sin cambios (el anadido es puramente aditivo)."
    - "Ambos validadores (skill/validate.mjs y su espejo app/src/content/validate.mjs) exigen conjugation cuando pos==='verbo', validan las formas y NO divergen entre si."
    - "structure.md obliga a emitir el paradigma masu por verbo, conjugando por grupo (ichidan/godan/irregular) y marcando irregulares, con la regla dura de nunca inventar formas."
    - "Los tipos TS de la app regenerados desde el schema incluyen la interfaz Conjugation."
  artifacts:
    - path: content/schema/content.schema.json
      provides: "defs.conjugation + defs.conjForm y vocab.conjugation opcional"
      contains: "masendeshita"
    - path: skill/validate.mjs
      provides: "checkConjugation + regla pos==='verbo' requiere conjugation"
      contains: "checkConjugation"
    - path: app/src/content/validate.mjs
      provides: "mismo checkConjugation (espejo identico)"
      contains: "checkConjugation"
    - path: app/src/content/content.ts
      provides: "interfaz Conjugation generada desde el schema"
      contains: "Conjugation"
    - path: skill/prompts/structure.md
      provides: "seccion que obliga a emitir el paradigma masu por verbo"
      contains: "conjugation"
  key_links:
    - from: "content/schema/content.schema.json vocab.conjugation"
      to: "defs.conjugation"
      via: "ref"
      pattern: "conjugation"
    - from: "skill/validate.mjs checkConjugation"
      to: "app/src/content/validate.mjs checkConjugation"
      via: "semantica identica (diff solo difiere en rutas/comentarios)"
      pattern: "checkConjugation"
---

<objective>
Extender el contrato de contenido (aditivo) para que un vocab de tipo verbo lleve su paradigma masu completo como dato estructurado, y hacer que la skill lo emita siempre. NO se toca la app salvo el espejo del validador y los tipos TS regenerados (el render es Phase 6).

Purpose: dejar el contrato listo para que Phase 6 renderice la tabla de conjugacion, sin romper a ningun consumidor existente (cambio ADITIVO y OPCIONAL en el schema; obligatorio-para-verbos solo en la capa semantica del validador, con el backfill en la misma fase, plan 05-02).
Output: schema con conjugation, ambos validadores en espejo, structure.md actualizado y content.ts regenerado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

INTERFACES (estructura EXACTA a anadir; schema draft 2020-12, additionalProperties:false en todas partes):

Anadir a defs en content/schema/content.schema.json dos nuevas defs. conjForm es un objeto con required ["kana","romaji"], additionalProperties:false, y properties: kanji (string minLength 1, OPCIONAL), kana (string minLength 1), romaji (string minLength 1). conjugation es un objeto con required ["group","dictionary","masu","masen","mashita","masendeshita"], additionalProperties:false, y properties: group (enum ["ichidan","godan","irregular"]), irregular (boolean, opcional), y dictionary/masu/masen/mashita/masendeshita cada uno con $ref "#/$defs/conjForm".

JSON literal a pegar dentro de $defs:

    "conjForm": {
      "type": "object",
      "required": ["kana", "romaji"],
      "additionalProperties": false,
      "properties": {
        "kanji":  { "type": "string", "minLength": 1 },
        "kana":   { "type": "string", "minLength": 1 },
        "romaji": { "type": "string", "minLength": 1 }
      }
    },
    "conjugation": {
      "type": "object",
      "required": ["group", "dictionary", "masu", "masen", "mashita", "masendeshita"],
      "additionalProperties": false,
      "properties": {
        "group":        { "enum": ["ichidan", "godan", "irregular"] },
        "irregular":    { "type": "boolean" },
        "dictionary":   { "$ref": "#/$defs/conjForm" },
        "masu":         { "$ref": "#/$defs/conjForm" },
        "masen":        { "$ref": "#/$defs/conjForm" },
        "mashita":      { "$ref": "#/$defs/conjForm" },
        "masendeshita": { "$ref": "#/$defs/conjForm" }
      }
    }

Y en $defs.vocab.properties anadir UNA clave (NO anadir a vocab.required, es opcional a nivel schema):

    "conjugation": { "$ref": "#/$defs/conjugation" }

Precedente aditivo identico a imitar: el campo blank en $defs.token (Phase 3, commit 3914594) — opcional, additionalProperties:false respetado, contenido previo intacto.

Decision del campo kanji en conjForm: es OPCIONAL. Para los verbos actuales (todos en kana) no se emite; se reserva para futuros verbos escritos con kanji (evita otro cambio de schema en Phase 6). Las formas SIEMPRE llevan kana+romaji (requeridos).

Decision mandatorio-vs-opcional: el schema lo deja OPCIONAL (retrocompatible); el validador lo hace OBLIGATORIO para pos==='verbo'. El backfill (plan 05-02, wave 2) deja el contenido valido dentro de la misma fase, asi que ningun verbo queda invalido.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema aditivo conjugation + regenerar tipos TS</name>
  <read_first>
    - content/schema/content.schema.json (el schema FROZEN; mira $defs.token con blank como precedente aditivo, y $defs.vocab)
    - app/src/content/content.ts (tipos generados; veras Token con blank? como precedente)
    - app/package.json (script gen:types: json2ts ../content/schema/content.schema.json -o src/content/content.ts)
  </read_first>
  <files>content/schema/content.schema.json, app/src/content/content.ts</files>
  <action>
    En content/schema/content.schema.json anadir a $defs las dos defs conjForm y conjugation EXACTAMENTE como el JSON literal del bloque CONTEXT/INTERFACES de este plan. En $defs.vocab.properties anadir la clave "conjugation": { "$ref": "#/$defs/conjugation" }. NO anadir conjugation a $defs.vocab.required (debe quedar opcional a nivel schema). NO tocar nada mas del schema (sigue siendo aditivo: additionalProperties:false intacto en vocab y en las nuevas defs).
    Luego regenerar los tipos TS de la app: desde app/ ejecutar npm run gen:types (produce app/src/content/content.ts con la interfaz Conjugation y conjugation? en Vocab).
  </action>
  <verify>
    <automated>cd /Users/pol/Documents/Other/JP-Learner && node -e "const s=require('./content/schema/content.schema.json'); const c=s['\$defs'].conjugation; if(!c) throw new Error('no conjugation def'); ['group','dictionary','masu','masen','mashita','masendeshita'].forEach(k=>{if(!c.required.includes(k)) throw new Error('missing '+k)}); if(s['\$defs'].vocab.required.includes('conjugation')) throw new Error('must be optional'); if(!s['\$defs'].vocab.properties.conjugation) throw new Error('vocab.conjugation missing'); console.log('SCHEMA OK')" && node skill/validate.mjs content/classes/2026-07-03.json</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"conjugation"' content/schema/content.schema.json es mayor o igual a 2 (la def + la propiedad en vocab).
    - grep -q '"masendeshita"' content/schema/content.schema.json es cierto.
    - El nodo de verificacion imprime SCHEMA OK (conjugation es def con las 5 formas + group requeridos, y NO esta en vocab.required).
    - grep -q 'Conjugation' app/src/content/content.ts es cierto (tipos regenerados) y grep -q 'conjugation' app/src/content/content.ts es cierto.
    - La clase existente sin verbos sigue validando: node skill/validate.mjs content/classes/2026-07-03.json imprime VALID 2026-07-03.
  </acceptance_criteria>
  <done>El schema tiene $defs.conjForm + $defs.conjugation y vocab.conjugation opcional; content.ts regenerado con la interfaz Conjugation; el contenido no-verbo sigue validando.</done>
</task>

<task type="auto">
  <name>Task 2: Validadores en espejo — conjugation semantica + obligatorio para verbos</name>
  <read_first>
    - skill/validate.mjs (el gate productor; fijate en checkTokens/checkSentence y en el bloque if (type === 'vocab') dentro de check)
    - app/src/content/validate.mjs (el ESPEJO; hoy es semanticamente identico linea por linea salvo la ruta del schema y el texto de usage)
  </read_first>
  <files>skill/validate.mjs, app/src/content/validate.mjs</files>
  <action>
    Anadir a AMBOS ficheros, con logica IDENTICA (misma funcion, mismos mensajes), la funcion checkConjugation y su invocacion desde la rama vocab de check.

    Funcion a anadir justo despues de checkSentence (identica en los dos ficheros):

      function checkConjugation(c, where) {
        const forms = ['dictionary', 'masu', 'masen', 'mashita', 'masendeshita'];
        for (const f of forms) {
          if (!c[f] || !c[f].kana || !c[f].romaji) throw new Error(`conjugation.${f} incomplete: ${where}`);
        }
        if (!['ichidan', 'godan', 'irregular'].includes(c.group)) throw new Error(`conjugation.group invalid: ${where}`);
        if (c.irregular !== undefined && c.irregular !== (c.group === 'irregular'))
          throw new Error(`conjugation.irregular inconsistent with group: ${where}`);
      }

    En la rama if (type === 'vocab') { ... } de check, DESPUES de las lineas existentes checkTokens/checkSentence, anadir estas dos lineas (identicas en los dos ficheros):

        if (it.pos === 'verbo' && !it.conjugation) throw new Error(`verb missing conjugation: ${it.id}`);
        if (it.conjugation) checkConjugation(it.conjugation, it.id);

    Regla dura: el bloque checkConjugation y las dos lineas nuevas deben ser BYTE-identicos en los dos ficheros. Los unicos puntos donde los dos ficheros pueden diferir son la ruta relativa del schema en schemaUrl y el string de usage del bloque CLI (ya difieren hoy). No cambies nada mas.
  </action>
  <verify>
    <automated>cd /Users/pol/Documents/Other/JP-Learner && test $(grep -c 'checkConjugation' skill/validate.mjs) -ge 2 && test $(grep -c 'checkConjugation' app/src/content/validate.mjs) -ge 2 && diff <(grep -v -e 'schemaUrl' -e 'usage:' -e 'validate.mjs <' skill/validate.mjs) <(grep -v -e 'schemaUrl' -e 'usage:' -e 'validate.mjs <' app/src/content/validate.mjs) && echo MIRROR_OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'checkConjugation' skill/validate.mjs es mayor o igual a 2 (definicion + invocacion) e identico en app/src/content/validate.mjs.
    - El diff (excluyendo lineas schemaUrl/usage/CLI) no imprime nada y luego imprime MIRROR_OK: los dos validadores no divergen en semantica.
    - Regla obligatoria-para-verbos activa (comportamiento ESPERADO en wave 1, lo arregla 05-02): node skill/validate.mjs content/classes/2026-07-13.json imprime un error que empieza por 'verb missing conjugation: 2026-07-13:vocab:okiru' y sale con codigo distinto de 0.
    - Una clase sin verbos sigue validando: node skill/validate.mjs content/classes/2026-07-03.json imprime VALID 2026-07-03.
  </acceptance_criteria>
  <done>Ambos validadores tienen checkConjugation identico, exigen conjugation para pos verbo, y no divergen; las clases sin verbos siguen validando.</done>
</task>

<task type="auto">
  <name>Task 3: structure.md — emitir el paradigma masu por verbo</name>
  <read_first>
    - skill/prompts/structure.md (seccion 4 vocab, y como Phase 3 documento el marcador blank en la seccion 5 con ejemplo trabajado)
    - skill/LEARNINGS.md (regla de contenido "Verbos: tabla de conjugacion completa" — AUTORITATIVA sobre las reglas de conjugacion por grupo)
    - content/schema/content.schema.json (el campo conjugation recien anadido, para citar la forma exacta)
  </read_first>
  <files>skill/prompts/structure.md</files>
  <action>
    Anadir a skill/prompts/structure.md, dentro de la seccion 4 (vocab) o como sub-seccion "4b. Verbos — tabla de conjugacion (conjugation)", una instruccion DURA: para CADA vocab con pos "verbo", emitir el objeto conjugation con:
    - group: uno de ichidan | godan | irregular.
    - irregular: true SOLO para する / くる y compuestos con する (p.ej. べんきょうする). Para ichidan/godan puede omitirse o poner false.
    - las 5 formas dictionary, masu, masen, mashita, masendeshita, cada una con kana + romaji (Hepburn). El kanji por forma es opcional (solo si el verbo se escribe con kanji).

    Reglas de conjugacion por grupo (documentar explicitamente):
    - ichidan (verbos en る tipo eru/iru): quitar る y anadir ます/ません/ました/ませんでした. Ej.: たべる->たべます; ねる->ねます; おきる->おきます.
    - godan (verbos en -u): la kana final de la columna -u pasa a la columna -i y se anade ます. Ej.: のむ->のみます; はたらく->はたらきます; かう->かいます. Negativo/pasado sobre esa raiz -i: のみません / のみました / のみませんでした.
    - irregular: する->します/しません/しました/しませんでした; くる->きます/きません/きました/きませんでした; compuestos con する (べんきょうする->べんきょうします...).

    Regla dura (citar de LEARNINGS): NUNCA inventar formas ni lecturas. Si hay duda del grupo de un verbo, buscarlo antes de conjugar; ante duda, no adivinar. La revision humana es solo de cobertura/lecturas, no de decisiones de gramatica.

    Incluir un ejemplo trabajado (JSON) con たべる (ichidan):

      "pos": "verbo",
      "conjugation": {
        "group": "ichidan",
        "dictionary":   { "kana": "たべる",           "romaji": "taberu" },
        "masu":         { "kana": "たべます",         "romaji": "tabemasu" },
        "masen":        { "kana": "たべません",       "romaji": "tabemasen" },
        "mashita":      { "kana": "たべました",       "romaji": "tabemashita" },
        "masendeshita": { "kana": "たべませんでした", "romaji": "tabemasen deshita" }
      }

    Nota a incluir: el validador rechaza un verbo sin conjugation (pos==='verbo' obligatorio a partir de ahora).
  </action>
  <verify>
    <automated>cd /Users/pol/Documents/Other/JP-Learner && grep -q 'conjugation' skill/prompts/structure.md && grep -q 'ichidan' skill/prompts/structure.md && grep -q 'godan' skill/prompts/structure.md && grep -q 'irregular' skill/prompts/structure.md && grep -q 'masendeshita' skill/prompts/structure.md && echo STRUCTURE_OK</automated>
  </verify>
  <acceptance_criteria>
    - skill/prompts/structure.md contiene una instruccion para emitir conjugation por verbo (grep -q 'conjugation').
    - Estan documentados los tres grupos: grep -q 'ichidan', grep -q 'godan', grep -q 'irregular' (todos ciertos).
    - Aparece la regla dura de no inventar formas (grep -qi 'inventar' o 'nunca').
    - Aparece el ejemplo trabajado con las 5 formas (grep -q 'masendeshita').
    - El comando de verify imprime STRUCTURE_OK.
  </acceptance_criteria>
  <done>structure.md obliga a emitir el paradigma masu por verbo, con reglas por grupo, marca de irregulares, regla de no inventar y un ejemplo trabajado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skill/app (LLM/humano) -> content JSON | El contenido lo produce la skill (LLM) + revision humana; entra al repo solo tras pasar el gate validate.mjs (fail-closed). No hay entrada de red ni de terceros: app offline, uso personal. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | content/schema/content.schema.json (cambio no-aditivo rompe consumidores) | mitigate | Cambio estrictamente aditivo (nuevas defs + una propiedad opcional); verify Task 1 confirma que conjugation NO esta en vocab.required y que una clase existente sigue validando. |
| T-05-02 | Tampering | skill/validate.mjs vs app/src/content/validate.mjs (divergencia de los espejos) | mitigate | Task 2 exige diff sin divergencia (MIRROR_OK) entre los dos validadores salvo rutas/usage. |
| T-05-03 | Information Disclosure | secretos/PII | accept | Sin backend, sin claves, sin datos personales; contenido de estudio de japones. Bajo valor. |
</threat_model>

<verification>
- node skill/validate.mjs content/classes/2026-07-03.json imprime VALID (retrocompatibilidad de una clase sin verbos).
- El schema pasa la comprobacion de estructura (SCHEMA OK).
- Los dos validadores no divergen (MIRROR_OK).
- structure.md documenta conjugation + los tres grupos + regla de no inventar (STRUCTURE_OK).
- app/src/content/content.ts contiene la interfaz Conjugation.
- NOTA esperada: tras este plan, las clases 2026-07-13 y 2026-07-15 quedan INVALIDAS (verbos sin conjugation). Es intencional; el plan 05-02 (wave 2) las deja validas.
</verification>

<success_criteria>
- content/schema/content.schema.json: campo conjugation aditivo y opcional; contenido no-verbo intacto.
- skill/validate.mjs y app/src/content/validate.mjs: checkConjugation identico + obligatorio para pos verbo.
- skill/prompts/structure.md: obliga a emitir el paradigma masu por verbo, con reglas por grupo e irregulares.
- app/src/content/content.ts regenerado con la interfaz Conjugation.
</success_criteria>

<output>
After completion, create .planning/phases/05-verbos-conjugaci-n-contenido-y-schema/05-01-SUMMARY.md
</output>
