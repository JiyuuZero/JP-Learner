---
phase: 01-jp-learner-v1-complete-pwa-content-skill
plan: 05
type: execute
wave: 4
depends_on: [04]
files_modified:
  - app/src/display/ruby.tsx
  - app/src/display/JapaneseText.tsx
  - app/src/exercises/check.ts
  - app/src/exercises/check.test.ts
  - app/src/exercises/generators.ts
  - app/src/exercises/generators.test.ts
  - app/src/exercises/Flashcard.tsx
  - app/src/exercises/MultipleChoice.tsx
  - app/src/exercises/Typing.tsx
  - app/src/exercises/WordBank.tsx
  - app/src/exercises/Matching.tsx
  - app/src/views/Session.tsx
  - app/src/views/Glosario.tsx
  - app/src/views/Guardados.tsx
  - app/src/progress/ProgressContext.tsx
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-04, EXER-01, EXER-02, EXER-03, EXER-04, EXER-05, EXER-06, UI-02]
must_haves:
  truths:
    - "Japanese renders via native <ruby> for Modes A (romaji over), B (progressive kana->kanji-with-furigana as kanji learned), and C (kanji only), from schema tokens with zero in-app NLP"
    - "Mode B substitutes a kanji run only when EVERY kanji char in that run's kanji[] is in the learned set; otherwise it renders the kana reading (gradual substitution, DISP-03)"
    - "The user can mark/unmark kanji as learned from item detail; auto-marked (FSRS-graduated) kanji show pre-marked (DISP-04, D-03)"
    - "All 5 exercise types (flashcard, multiple-choice, typing, word-bank, matching) render, feed the ONE shared FSRS card via gradeItem, and are practicable JA->ES and ES->JA (EXER-06)"
    - "The word-bank exercise (EXER-04) splits chips from authored sentence.tokens[] and runs on the pre-skill sample (the norikomu example carries tokens from Plan 01) — never in-app segmentation"
    - "Typing uses a tolerant checker (romaji/kana variants, n/nn); distractors come from same-class vocab (never random)"
    - "The session frame drives the hybrid session (D-01): automatic queue from buildSession, plus a manual exercise-type pick"
    - "The Guardar/heart toggle on Glosario rows persists a favorite flag, and the Guardados view lists those favorited items (the heart has a destination — UI-02)"
  artifacts:
    - path: "app/src/display/ruby.tsx"
      provides: "renderTokens(tokens, mode, learned, romajiVisible) - pure A/B/C rendering (DISP-01/02/03)"
      exports: ["renderTokens"]
    - path: "app/src/exercises/check.ts"
      provides: "isAnswerCorrect tolerant typing checker (EXER-03)"
      exports: ["isAnswerCorrect"]
    - path: "app/src/exercises/generators.ts"
      provides: "5 pure exercise generators + pickDistractors + direction (EXER-01..06)"
      exports: ["pickDistractors"]
    - path: "app/src/views/Session.tsx"
      provides: "shared session frame: progress bar + big action button + hybrid D-01 flow"
      contains: "Comprobar"
    - path: "app/src/views/Guardados.tsx"
      provides: "list of favorited/saved items (the heart-toggle destination, UI-02)"
      contains: "Guardados"
  key_links:
    - from: "app/src/display/ruby.tsx"
      to: "learnedSet (ProgressContext) + schema tokens"
      via: "renderTokens(t.kanji every in learned)"
      pattern: "learned\\.has|every"
    - from: "app/src/exercises/*.tsx"
      to: "gradeItem (ProgressContext.grade)"
      via: "answer -> Grade -> shared FSRS card"
      pattern: "grade\\(|Rating\\.|Again|Good"
    - from: "app/src/views/Session.tsx"
      to: "buildSession (Plan 04)"
      via: "automatic queue + manual type pick (D-01)"
      pattern: "buildSession"
    - from: "app/src/views/Guardados.tsx"
      to: "favoriteSet (ProgressContext)"
      via: "filter content by favorited id"
      pattern: "favorite|favoriteSet"
---

<objective>
Build the practice surface: native `<ruby>` rendering for display Modes A/B/C (pure functions over schema tokens + the learned-kanji set - zero in-app Japanese analysis), the learned-kanji marking UI (auto + manual, D-03/DISP-04), all 5 exercise generators + components feeding the ONE shared FSRS card bidirectionally (EXER-01..06), a tolerant typing checker (romaji/kana, n/nn), the shared session frame implementing the hybrid session (D-01), and the Guardar/favorites path (heart toggle on Glosario rows -> Guardados view destination, UI-02).

Purpose: This turns the frozen content + progress state into effective active-recall practice - the Core Value. All Japanese analysis was pushed into the schema/skill; here it is pure rendering + pure transforms. The word-bank (the signature exercise) draws its chips from authored `sentence.tokens[]`; the Plan-01 sample now carries those tokens on the norikomu example, so EXER-04 demonstrably runs BEFORE the skill has authored any class. Descope order note: Mode C is a cut candidate before SRS correctness, but it is cheap (kanji-only, no rt) so build it; the 5 exercises are table stakes and are NOT cut.
Output: ruby rendering, JapaneseText component, tolerant checker, 5 exercise generators + components, the Session frame, a Glosario upgraded to render Japanese per the active mode with learned-kanji + favorite toggles, and the Guardados view listing favorited items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-RESEARCH.md
@.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-UI-SPEC.md
@.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-CONTEXT.md
@.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-02-SUMMARY.md
@.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-04-SUMMARY.md

<interfaces>
<!-- Verbatim from 01-RESEARCH.md. Consumes Plan 01 types + Plan 04 progress. -->
Token (Plan 01): { surface:string; reading:string; isKanji:boolean; kanji?:string[] }
Sentence (Plan 01): { kanji:string; kana:string; romaji:string; es:string; tokens?:Token[] }  // tokens present on the norikomu example (added in Plan 01) -> word-bank chip source
renderTokens (verbatim, 01-RESEARCH.md Ruby Rendering):
  renderTokens(tokens: Token[], mode:'A'|'B'|'C', learned: Set<string>, romajiVisible: boolean): ReactNode[]
  - !isKanji token -> literal span
  - Mode C -> span(surface), no rt
  - Mode A -> if romajiVisible: <ruby>surface<rp>(</rp><rt>reading|romaji</rt><rp>)</rp></ruby> else span(surface)
  - Mode B -> known = t.kanji.every(k => learned.has(k)); known ? <ruby>surface + kana rt</ruby> : span(reading)
  Mode A romaji granularity: apply romaji rt at WORD level (schema stores word-level romaji, not per-token) - A1 assumption.

Progress (Plan 04 ProgressContext): grade(itemId, itemType, classId, grade), learnedSet:Set<string>, markLearned(char), unmarkLearned(char), meta.displayMode, meta.romajiVisible; buildSession(store, srsByItem, scope, subMode, now) from session.ts.
Favorites (extend ProgressContext in THIS plan, Task 3): favoriteSet:Set<string> (item ids), toggleFavorite(itemId), persisted via the existing progress `meta` store (add a `favorites:string[]` field to ProgressMeta - additive, no APP_VERSION bump needed since idb reads missing fields as undefined -> default []). The Glosario heart writes toggleFavorite; the Guardados view reads favoriteSet.
Grade mapping (01-RESEARCH.md): Flashcard = 4 self-grade buttons (Otra vez/Difícil/Bien/Fácil = Again/Hard/Good/Easy); Typing/MC/WordBank/Matching = wrong->Again, correct->Good.
Direction (EXER-06): 'JA_ES' | 'ES_JA' - swaps prompt/answer; header indicator, no separate screen.
Distractors: sampled from SAME classId vocab (never random). Tolerant checker: accepted = [item.kana, item.romaji] (+kanji); ES direction matches es string loosely.

UI-SPEC session frame: top back-arrow + title + points pill; numbered progress bar (indigo active); large rounded card body; bottom full-width 56px pill (Comprobar -> Seguir). Feedback copy: correct "¡Bien!"; incorrect "Casi. La respuesta es: {answer}". Word-bank = the signature exercise (ref_3): dashed slots + pastel chips, indigo when placed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ruby.tsx (A/B/C) + tolerant checker + exercise generators, with tests</name>
  <read_first>
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-RESEARCH.md Ruby Rendering (lines ~737-774), Exercise Engines (lines ~778-821)
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-UI-SPEC.md Ruby rendering contract
    - app/src/content/content.ts (Token/Vocab/Sentence types), app/src/progress/srs.ts (grade mapping)
    - content/classes/2026-04-14.json (the norikomu example.tokens the word-bank test will consume)
  </read_first>
  <behavior>
    - check.test.ts (isAnswerCorrect): "tabemasu"==="tabemasu"; n/nn folding ("konnichiha"==="konnichiwa"? no - test n/nn: "sennsei"==="sensei"); shi/si, tsu/tu, chi/ti equivalence; punctuation/space/long-vowel-dash stripped; NFKC full/half-width unify; kana input matches item.kana; a clearly-wrong answer returns false.
    - generators.test.ts: pickDistractors returns N items from the same-class pool excluding the target, never the target, never fewer than requested when the pool allows; multipleChoice produces exactly 4 options incl. the correct one; wordBank chip set = correct tokens (split from sentence.tokens[]) + 1-2 distractors, and validation passes only for the correct order; a wordBank built from the norikomu example (which HAS authored tokens from Plan 01) yields chips whose correct order reconstructs 電車に乗り込む。; direction 'ES_JA' vs 'JA_ES' swaps prompt/answer sides.
    - ruby (render test with @testing-library/react or a pure-node assertion of the returned nodes): Mode C renders surface with no rt; Mode B renders kana span when a kanji is NOT in learned, and a <ruby> with kana rt when ALL kanji in the run are learned; the 毎日 token (kanji ["毎","日"]) substitutes only when both are learned; the 乗り込む tokens render 乗/込 as ruby (when learned) and り/む always as literal kana.
  </behavior>
  <action>
    1. Add `@testing-library/react@16` + `jsdom` (or happy-dom) as dev deps in `app/` and configure vitest environment jsdom for the ruby render test (keep check/generators tests as pure logic).
    2. Write `app/src/display/ruby.tsx` VERBATIM from 01-RESEARCH.md Ruby Rendering: `renderTokens(tokens, mode, learned, romajiVisible)`. Mode B `known = (t.kanji ?? []).every(k => learned.has(k))`. Include `<rp>` fallback parens. Mode A applies romaji rt at word granularity (A1). NEVER compute readings; only read from tokens.
    3. Write `app/src/display/JapaneseText.tsx`: a component that takes a Vocab/Sentence + consumes ProgressContext (displayMode, romajiVisible, learnedSet) and renders via renderTokens. Used by Glosario, exercises, and item detail. Line-height >= 1.5 on ruby-bearing text; rt gap 4px; rt ~58% size, muted color (UI-SPEC).
    4. Write `app/src/exercises/check.ts` VERBATIM from 01-RESEARCH.md Tolerant typing checker: `isAnswerCorrect(input, accepted)` with the exact normalization chain (trim/lowercase, strip punctuation/space, NFKC, nn->n, shi->si, chi->ti, tsu->tu, fu->hu, ji->zi, sha/shu/sho + cha/chu/cho foldings, strip long-vowel dash, collapse doubled consonants). accepted = [item.kana, item.romaji]; ES direction matches the es string loosely.
    5. Write `app/src/exercises/generators.ts`: pure generators for all 5 types + `pickDistractors(pool, exclude, n)` (shuffle same-class pool minus target, take n) + a `Direction` type. Each generator input = (item, sameClassPool, direction), output = an exercise instance descriptor the components render. Word-bank: split the example sentence into chips from its `tokens[]` (the schema/authored source; the Plan-01 sample provides them on the norikomu example) — NEVER in-app JA segmentation (A2). If a chosen example lacks tokens, the generator MUST skip it / pick another item that has tokens rather than segmenting in-app. Include 1-2 same-class distractor chips; validation = placed order === correct token order.
    6. Write check.test.ts, generators.test.ts, and the ruby render test per <behavior>. GREEN: `npm test` passes.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'export function renderTokens' app/src/display/ruby.tsx` succeeds.
    - `grep -q '<rp>' app/src/display/ruby.tsx` succeeds (ruby fallback parens) AND `grep -q 'every' app/src/display/ruby.tsx` succeeds (Mode B per-run learned check).
    - ruby.tsx does NO reading computation: `grep -riq 'kuromoji\|kuroshiro\|wanakana' app/src/display/ && exit 1 || true` (no JA NLP libs).
    - `grep -q 'export function isAnswerCorrect' app/src/exercises/check.ts` succeeds AND `grep -q "replace(/nn/g" app/src/exercises/check.ts` succeeds (n/nn folding).
    - `grep -q 'pickDistractors' app/src/exercises/generators.ts` succeeds AND `grep -q 'ES_JA' app/src/exercises/generators.ts` AND `grep -q 'JA_ES' app/src/exercises/generators.ts` (bidirectional).
    - Word-bank chips come from tokens, not in-app segmentation: `grep -q 'tokens' app/src/exercises/generators.ts` succeeds AND `grep -riq 'kuromoji\|kuroshiro\|wanakana\|segment' app/src/exercises/generators.ts && exit 1 || true`.
    - `cd app && npm test` exits 0 with the ruby Mode B 毎日 substitution test, the n/nn checker test, the pickDistractors same-class test, and the wordBank-from-norikomu-tokens test (reconstructs 電車に乗り込む。) all passing.
  </acceptance_criteria>
  <done>renderTokens implements A/B/C with per-run Mode B substitution and no NLP; isAnswerCorrect folds romaji/kana variants; 5 generators + same-class distractors + bidirectional direction are pure and tested; the word-bank builds chips from authored sentence.tokens and runs on the pre-skill norikomu example; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: 5 exercise components + Session frame (hybrid D-01) + Glosario/detail with learned + favorite toggles</name>
  <read_first>
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-UI-SPEC.md Practice session, Exercise-type bodies, Library/Glossary, Item detail, Shared component tokens, Copywriting Contract (feedback + grade buttons + sub-mode selector)
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-CONTEXT.md D-01, D-02, D-04
    - app/src/exercises/generators.ts, app/src/exercises/check.ts, app/src/display/JapaneseText.tsx (Task 1)
    - app/src/progress/ProgressContext.tsx, app/src/progress/session.ts (Plan 04)
  </read_first>
  <files>app/src/exercises/Flashcard.tsx, app/src/exercises/MultipleChoice.tsx, app/src/exercises/Typing.tsx, app/src/exercises/WordBank.tsx, app/src/exercises/Matching.tsx, app/src/views/Session.tsx, app/src/views/Glosario.tsx</files>
  <action>
    1. Write the 5 exercise components (hand-rolled per UI-SPEC §Exercise-type bodies), each rendering JA via JapaneseText per active mode, each ending by calling ProgressContext.grade(itemId, itemType, classId, grade):
       - `Flashcard.tsx` (EXER-01): large lavender card (#E7E7FC) with the term (JA per mode) + "En japonés/En español" sublabels + a favorite heart (top-right, calls toggleFavorite) + a Pronunciación speaker bar (TTS wired in Plan 06 - render the button slot now, hidden if no voice). Tap to reveal back (translation + example + TTS). Then 4 self-grade pill buttons Otra vez/Difícil/Bien/Fácil -> Again/Hard/Good/Easy.
       - `MultipleChoice.tsx` (EXER-02): prompt card + 4 option chips (2-col grid); correct fills indigo, wrong fills coral briefly; distractors from same-class vocab (generators.pickDistractors). wrong->Again, correct->Good.
       - `Typing.tsx` (EXER-03): prompt card + text input (dashed underline) + Comprobar; isAnswerCorrect(input, accepted); feedback "¡Bien!" / "Casi. La respuesta es: {answer}". wrong->Again, correct->Good.
       - `WordBank.tsx` (EXER-04, the signature exercise, follow ref_3): JA sentence with dashed gap slot(s) + wrapped pastel chips (rounded-2xl, alternating tints) built from the generator's token chips; tapping a chip fills the slot indigo, tapping again returns it; 1-2 distractor chips; Comprobar validates order.
       - `Matching.tsx` (EXER-05): two columns (ES / JA) of ~5 cards; tap one then its pair; matched pairs clear; coarse Good/Again per pair.
       - All support EXER-06 direction: a small header direction indicator; prompt/answer sides swap per the generator's direction.
    2. Write `app/src/views/Session.tsx` - the shared session frame (UI-SPEC §Practice session): top back-arrow (ArrowLeft) + centered title + points pill; numbered progress bar 1..N (indigo active) under the header; the current exercise body in a large rounded card; bottom full-width 56px pill (Comprobar -> Seguir), which replaces the bottom nav (hide BottomNav during a session). Implement the hybrid session (D-01): default AUTOMATIC mode uses buildSession(store, srsByItem, scope, subMode, now) to build the queue and rotates the exercise type per item (simple rotation/random - NOT by maturity); the user can also pick a specific exercise type (manual mode) over the same queue. Reviews-before-new + the new-card cap come from buildSession. Session start is parameterized by scope (Hoy/Semana/Total) + subMode (Repaso SRS / Repasar periodo) passed from the launcher (Home dashboard wires this in Plan 06; here accept them via route/props/state with sensible defaults so Session is testable standalone). On finish show "Terminar sesión".
    3. Upgrade `app/src/views/Glosario.tsx` (D-04, DISP-01..04): two switchable views (segmented toggle, indigo active) - Por clase (grouped by date -> vocab/gramática/notas) and Por categoría (Vocabulario / Gramática) - plus a global search field ("Sin resultados" empty state per UI-SPEC). Each vocab row renders JA via JapaneseText (active mode + `<ruby>`), romaji, ES, and a Guardar/heart toggle that calls ProgressContext.toggleFavorite(item.id) and reflects favoriteSet.has(item.id). Tapping opens item detail: kanji·kana·romaji·es·example (all per active mode), a TTS speaker slot, and a per-kanji "Aprendido" golden-star toggle (markLearned/unmarkLearned) - auto-marked (FSRS-graduated, source:'auto') kanji show the star pre-filled (DISP-03/04, D-03).
    4. Confirm no dangerouslySetInnerHTML anywhere; build + typecheck.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All 5 exercise component files exist: `for f in Flashcard MultipleChoice Typing WordBank Matching; do test -f app/src/exercises/$f.tsx || exit 1; done`.
    - Each exercise calls grade: `grep -rq 'grade(' app/src/exercises/*.tsx` succeeds; flashcard exposes 4 grades: `grep -q 'Otra vez' app/src/exercises/Flashcard.tsx` AND `grep -q 'Fácil' app/src/exercises/Flashcard.tsx`.
    - Typing uses the checker: `grep -q 'isAnswerCorrect' app/src/exercises/Typing.tsx` AND feedback copy present: `grep -q 'Casi. La respuesta es' app/src/exercises/Typing.tsx`.
    - Word-bank chips: `grep -q 'Comprobar' app/src/exercises/WordBank.tsx` (validates order) - the signature exercise present.
    - Session frame: `grep -q 'buildSession' app/src/views/Session.tsx` AND `grep -q 'Comprobar' app/src/views/Session.tsx` AND `grep -q 'Terminar sesión' app/src/views/Session.tsx`.
    - Glosario two views + detail: `grep -q 'Por clase' app/src/views/Glosario.tsx` AND `grep -q 'Por categoría' app/src/views/Glosario.tsx` AND `grep -iq 'aprendido' app/src/views/Glosario.tsx` (learned toggle).
    - Glosario heart writes favorites: `grep -q 'toggleFavorite' app/src/views/Glosario.tsx` succeeds (the heart has a persistence path feeding Guardados).
    - JapaneseText consumed (not raw text): `grep -rq 'JapaneseText' app/src/views/Glosario.tsx` AND `grep -rq 'JapaneseText' app/src/exercises/` succeed.
    - No dangerouslySetInnerHTML: `grep -rq 'dangerouslySetInnerHTML' app/src/exercises/ app/src/display/ app/src/views/Session.tsx app/src/views/Glosario.tsx && exit 1 || true`.
    - `cd app && npx tsc --noEmit` and `npm run build` and `npm test` all exit 0.
  </acceptance_criteria>
  <done>All 5 exercises render per display mode and feed the shared FSRS card bidirectionally; the Session frame drives the hybrid D-01 flow via buildSession; Glosario has the two D-04 views + detail with auto+manual learned-kanji toggles driving Mode B AND a favorite/heart toggle wired to toggleFavorite; build/typecheck/tests pass; no dangerouslySetInnerHTML.</done>
</task>

<task type="auto">
  <name>Task 3: Favorites in ProgressContext + Guardados view (the heart's destination, UI-02)</name>
  <read_first>
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-UI-SPEC.md §Library/Glossary (list rows), §Copywriting Contract (empty-state copy), §Shared component tokens (list rows)
    - .planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-04-SUMMARY.md (ProgressContext API + meta store shape)
    - app/src/progress/ProgressContext.tsx (Plan 04 - the context to extend), app/src/progress/db.ts (ProgressMeta shape)
    - app/src/views/Glosario.tsx (the vocab item row to reuse), app/src/display/JapaneseText.tsx (render JA per mode)
  </read_first>
  <files>app/src/progress/ProgressContext.tsx, app/src/views/Guardados.tsx</files>
  <action>
    1. Extend `app/src/progress/ProgressContext.tsx` (minimal, additive) with favorites:
       - Add a `favorites: string[]` field to the `ProgressMeta` persisted in the `meta` store (additive; a pre-existing meta record without the field reads as `undefined` -> default to `[]`; NO APP_VERSION bump required since idb tolerates missing fields and the upgrade path is additive).
       - Expose on the context: `favoriteSet: Set<string>` (derived from meta.favorites) and `toggleFavorite(itemId: string): void` which flips membership, persists the updated `favorites` array to the meta store, and updates in-memory state. Favorites are item ids (e.g. `2026-04-14:vocab:norikomu`) referencing content by ID only (PROG-04 - no content text stored).
       - Keep the existing context surface (grade/markLearned/etc.) untouched.
    2. Replace the `Guardados.tsx` placeholder (from Plan 02) with a real view that lists favorited items:
       - Consume `favoriteSet` from ProgressContext and the ContentStore (from ContentContext, Plan 02) to resolve each favorited id to its Vocab/Grammar item (via vocabById/grammarById maps).
       - Title "Guardados". Render each favorited item as a row REUSING the same Glosario vocab-row layout (JA via JapaneseText per active display mode + `<ruby>`, romaji, ES) plus the Guardar/heart toggle (filled, since it is a saved item) that calls toggleFavorite to un-save (row disappears when un-saved).
       - Empty state (no favorites yet): a friendly Spanish empty state — heading "Sin nada guardado" + body "Toca el corazón en el glosario para guardar palabras aquí." (consistent with the UI-SPEC empty-state pattern; Spanish per D-05).
       - No `dangerouslySetInnerHTML`; content renders only as React text / `<ruby>`.
    3. typecheck + build + tests.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - ProgressContext exposes favorites: `grep -q 'toggleFavorite' app/src/progress/ProgressContext.tsx` succeeds AND `grep -q 'favoriteSet' app/src/progress/ProgressContext.tsx` succeeds AND `grep -q 'favorites' app/src/progress/ProgressContext.tsx` succeeds (persisted array).
    - Guardados renders favorites, not a placeholder: `grep -q 'favoriteSet' app/src/views/Guardados.tsx` succeeds AND `grep -q 'JapaneseText' app/src/views/Guardados.tsx` succeeds (reuses the Glosario row rendering) AND `grep -q 'Guardados' app/src/views/Guardados.tsx` succeeds.
    - Un-save path present: `grep -q 'toggleFavorite' app/src/views/Guardados.tsx` succeeds (tapping the filled heart removes the item).
    - Empty-state copy present: `grep -q 'Sin nada guardado' app/src/views/Guardados.tsx` succeeds.
    - Favorites are by-ID only (PROG-04): the meta favorites are string ids — `grep -q 'string\[\]' app/src/progress/ProgressContext.tsx` OR the ProgressMeta favorites field is typed `string[]` in db.ts/context; no content text is stored in favorites.
    - No dangerouslySetInnerHTML: `grep -rq 'dangerouslySetInnerHTML' app/src/views/Guardados.tsx app/src/progress/ProgressContext.tsx && exit 1 || true`.
    - `cd app && npx tsc --noEmit` and `npm test` and `npm run build` all exit 0.
  </acceptance_criteria>
  <done>ProgressContext exposes favoriteSet + toggleFavorite persisted (additively) in the meta store; the Guardados view lists favorited items reusing the Glosario row (JA per mode) with an un-save heart and a Spanish empty state; the Glosario heart toggle now has a working destination (UI-02); build/typecheck/tests pass; favorites reference content by ID only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content JSON -> ruby/exercise/Guardados DOM | Content strings (kanji/kana/romaji/es/tokens) render into React elements and `<ruby>`. |
| user typed input -> checker | User input is compared, never executed. |
| favorites -> meta store | Favorite item ids (strings) are persisted locally; no content text crosses into the store (PROG-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | XSS via content rendered as HTML in `<ruby>`/cards/Guardados rows | mitigate | Render content ONLY as React text children / `<ruby>` from structured token fields; NEVER dangerouslySetInnerHTML on any content field (grep gate in acceptance across exercises/display/Session/Glosario/Guardados). React escapes by default. |
| T-05-02 | Tampering | Wrong furigana teaching wrong readings (Mode B linchpin) | mitigate | renderTokens reads authoritative readings from schema tokens only; NO in-app reading computation (grep gate forbids kuromoji/kuroshiro/wanakana); the 乗り込む/毎日 render tests prove correctness. The word-bank also draws chips from authored tokens (no in-app segmentation). |
| T-05-03 | Information disclosure | typed answers / progress / favorites leaked | accept | Single-user, local-only; input is compared in-memory, favorites store item ids only (no content), never transmitted (no network calls beyond same-origin content). |
</threat_model>

<verification>
- `npm test`: A/B/C rendering (incl. 毎日/乗り込む), tolerant checker, generator distractor/direction tests, and the word-bank-from-norikomu-tokens test pass.
- `npm run build` + typecheck pass.
- Manual (executor): switch display mode in Perfil and confirm Glosario re-renders A/B/C; mark a kanji learned and confirm Mode B substitutes only that run; tap the heart on a Glosario row and confirm it appears in Guardados, tap it again in Guardados and confirm it disappears; run one session of each exercise type (incl. the word-bank on the norikomu example) and confirm grading advances the shared card.
</verification>

<success_criteria>
- Japanese renders correctly in all 3 modes via native `<ruby>` from schema tokens (zero NLP); Mode B substitutes per-learned-kanji-run (gradual, D-03). All 5 exercise types work bidirectionally on the one shared FSRS card with tolerant typing + same-class distractors, inside the hybrid session frame (D-01); the word-bank runs on the pre-skill sample from authored tokens. Glosario provides the two D-04 views + item detail with auto+manual learned-kanji marking AND a favorite/heart toggle whose destination — the Guardados view — lists saved items (UI-02).
</success_criteria>

<output>
After completion, create `.planning/phases/01-jp-learner-v1-complete-pwa-content-skill/01-05-SUMMARY.md` documenting: renderTokens + JapaneseText API, the exercise component contracts + how they call grade, the word-bank's authored-token chip source, the favorites API added to ProgressContext (favoriteSet/toggleFavorite + the meta favorites field) and the Guardados view, the Session frame's launch parameters (scope + subMode) for Plan 06's dashboard to wire, and the TTS speaker slots left for Plan 06.
</output>
</output>
